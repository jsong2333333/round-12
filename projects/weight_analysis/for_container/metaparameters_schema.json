{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "Trojan Detection Container (ICSI)",    
    "technique": "Weight Analysis with Gradient Boosting Classifier",
    "technique_description": "Extracts the matrix-multiplied-weights for each model, select 39 features out of the 270 features; Trains the features on an RNN model.",
    "technique_changes": "Changing feature selection and the classifier. This container cannot support configuration mode because we're testing the RNN model we trained.",
    "commit_id": "",
    "repo_name": "https://github.com/jsong2333333/round-12",
    "required": [],
    "additionalProperties": false,
    "type": "object",
    "properties": {}
  }